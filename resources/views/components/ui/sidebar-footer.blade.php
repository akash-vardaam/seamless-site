@props([@props([











</div>    {{ $slot }}>    {{ $attributes }}    class="flex flex-col gap-2 p-2 {{ $class }}"    data-sidebar="footer"<div])    'class' => '',    'class' => '',
])

<div
    data-sidebar="footer"
    class="flex flex-col gap-2 p-2 {{ $class }}"
    {{ $attributes }}
>
    {{ $slot }}
</div>
